// create a function that takes in an array. i
//f the first number, is less thab the last number alert 'hi. 
//if the first number is greater than the last number aler 'bye'.
// if they are equal, alert 'we close in an hour'

function compareArray(arr){
if(arr[0] < arr [arr.length -1 ]){
alert('hi')
} else if ( arr[0] > arr[arr.length-1]){
    alert('Bye')
} else {
    aler('we are closed')

}
}

compareArray([5,2,3,4,1])
compareArray([5,2,3,4,5])